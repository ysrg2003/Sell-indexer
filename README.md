# 💎 LatestAI Indexer Pro | الدليل التشغيلي للنظام السحابي
## 🛡️ المستوى: احترافي (Commercial Edition)
### الحالة: مستقر (Stable v2.4)

مرحباً بك في نظام الإدارة التقنية لخدمة الأرشفة السريعة. تم تصميم هذا الدليل لضمان تشغيل النظام بأعلى كفاءة مع عزل كامل لبيانات العملاء وضمان مجانية الموارد السحابية بنسبة 100%.

---

## 🏗️ المخطط العام للنظام (System Architecture)
يعتمد النظام على تكامل أربعة أركان تقنية:
1. **Google Cloud Platform:** لتوفير القوة البرمجية للأرشفة (Indexing API).
2. **GitHub Actions:** كمحرك أتمتة سحابي (Cloud Runner) لا يستهلك موارد جهازك.
3. **JS Injection UI:** لوحة تحكم محلية مشفرة تتواصل مع الـ API مباشرة.
4. **Google Search Console:** حلقة الوصل النهائية لاعتماد الروابط.

---

## 📦 أولاً: دليل الموارد والمواد الخام (Source Directory)

| المورد | الموقع الرسمي | الدور الإستراتيجي |
| :--- | :--- | :--- |
| **Gmail Workspace** | [Google Accounts](https://accounts.google.com) | إدارة الهوية الرقمية (يفضل حساب لكل 10 عملاء VIP). |
| **GCP Console** | [Google Cloud](https://console.cloud.google.com) | تفعيل بروتوكولات الأرشفة وتوليد مفاتيح الوصول (JSON). |
| **GitHub Engine** | [GitHub Registry](https://github.com) | استضافة المستودعات العامة لتوفير كلفة الـ Actions. |
| **Python Core** | `indexer.py` | السكريبت المسؤول عن معالجة طلبات الـ JSON وتجاوز الكوتا. |
| **Action YAML** | `.github/workflows/main.yml` | جدولة المهام وضمان التنفيذ فور تحديث ملف الروابط. |

---

## 🛠️ ثانياً: البروتوكول التنفيذي (Deployment Steps)

### 1️⃣ تهيئة بيئة Google Cloud (المحرك السحابي)
* **إنشاء المشروع:** من القائمة العلوية اختر `New Project` > سمه باسم العميل الفريد.
* **تفعيل الخدمة:** ابحث عن `Indexing API` وقم بتفعيله (Enable).
* **حساب الخدمة (Service Account):** * انتقل إلى `Credentials` > `Create Credentials` > `Service Account`.
    * سمه `indexer-bot` وامنحه دور `Owner`.
* **توليد المفتاح:** بعد إنشاء الحساب، اضغط عليه > تبويب `Keys` > `Add Key` > `Create New Key` > `JSON`.
* **النتيجة:** سيتم تحميل ملف بصيغة `.json`. هذا الملف هو "التصريح الأمني" لدخول خوادم جوجل.

### 2️⃣ بناء البنية التحتية على GitHub
* **Repository:** أنشئ مستودعاً عاماً (Public) لتوفير دقائق التشغيل.
* **الملفات الأساسية:**
    * ارفع `indexer.py` (يحتوي على منطق الأرشفة وحماية الكوتا).
    * ارفع `urls.txt` (ملف فارغ يمثل قاعدة بيانات الروابط المؤقتة).
* **إعدادات الأمان (Secrets):** اذهب إلى `Settings` > `Actions` > `New repository secret`:
    1. **اسم السر:** `GOOGLE_INDEXING_JSON` | **القيمة:** الصق محتوى ملف الـ JSON كاملاً.
    2. **اسم السر:** `DAILY_LIMIT` | **القيمة:** رقم يمثل سعة باقة العميل (مثلاً `100`).
* **إذن التشغيل:** تأكد من تفعيل `Actions` من الإعدادات العامة للمستودع.

### 3️⃣ هندسة الوصول (The Security Token)
لتشغيل لوحة التحكم من جهاز العميل إلى GitHub:
1. من إعدادات حسابك (Settings) > `Developer settings` > `Personal access tokens`.
2. اختر `Fine-grained tokens` (أكثر أماناً من الـ Classic).
3. حدد مستودع العميل فقط (Selected Repositories).
4. امنح صلاحية `Contents` (تصريح: `Read and Write`).
5. **ملاحظة:** احتفظ بالتوكن في ملف خارجي؛ سيختفي بمجرد إغلاق الصفحة.

---

## 🖥️ ثالثاً: هندسة لوحة التحكم (The Control Panel)
يتم إعداد ملف `index.html` ليكون "تطبيقاً مستقلاً" يعمل لدى العميل:

1. **إعداد الكود:** افتح ملف الـ HTML وضع التوكن والمسار في قسم `CONFIG` بالأعلى.
2. **التمويه البرمجي:** في المستودعات العامة، لا ترفع هذا الملف أبداً. احفظه محلياً على جهازك.
3. **التصدير:** استخدم أي تطبيق (مثل WPS) لحفظ الملف بصيغة `.html`.
4. **التعديلات الدورية:** ابحث عن `const REPO_PATH` لتغيير مسار مستودع العميل في كل نسخة جديدة.

---

## 🔗 رابعاً: الربط مع العميل (The Final Handshake)
هذه هي "اللمسة الأخيرة" التي تجعل النظام يعمل:
1. اطلب من العميل الدخول إلى **Google Search Console**.
2. من الإعدادات > المستخدمون > إضافة مستخدم.
3. ضع البريد الإلكتروني المستخرج من **Service Account** (ينتهي بـ `.iam.gserviceaccount.com`).
4. **هام جداً:** يجب أن تكون الصلاحية `Owner` (مالك) وإلا سيرفض الـ API الطلبات.

---

## 📉 خامساً: نظام المتابعة والتوثيق (Master Tracking)
استخدم **Google Sheets** لإدارة إمبراطوريتك التقنية. الجدول المقترح:

| العميل | كود المستودع | سعة الباقة | تاريخ الانتهاء | بريد الـ Bot | الحالة التقنية |
| :--- | :--- | :--- | :--- | :--- | :--- |
| متجر X | `user/repo-x` | 200/يوم | 2024-12-01 | `bot@proj.iam...` | ✅ يعمل |

---

## 🆘 دليل استكشاف الأخطاء (Troubleshooting)
* **Status 401:** التوكن غير صحيح أو تم حذفه من قبل GitHub.
* **Status 403:** العميل لم يضف بريد الـ Service Account كـ Owner في الكونسول.
* **Status 429:** تم تجاوز الحد اليومي المسموح به من جوجل (200 رابط).
* **Actions لم يبدأ:** تأكد من أن ملف الـ `.yml` موجود في مسار `.github/workflows/`.

---
**💡 نصيحة الإدارة:** احتفظ بنسخة احتياطية من ملفات الـ JSON لكل عميل في مجلد مشفر على Google Drive للرجوع إليها عند الحاجة.
